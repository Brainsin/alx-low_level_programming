#include "main.h"
/**
 * main - entry
 *
 * return : 0
 */
int _abs(int a)
{
	if(a < 0)
	{
		return(-a);
	}
	else if(a == 0)
	{
		return(a);
	}
	else
	{
		return(a);
	}
	_putchar('\n');
	return(0);
}
