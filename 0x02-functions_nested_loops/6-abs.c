#include "main.h"
/**
 * _abs - print that absolute value of a number
 * @a: represent an integer
 * Return: always 0
 */
int _abs(int a)
{
	if (a < 0)
	{
		return (-a);
	}
	else if (a == 0)
	{
		return (a);
	}
	else
	{
		return (a);
	}
	_putchar('\n');
	return (0);
}
