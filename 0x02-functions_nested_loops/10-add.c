#include "main.h"
/**
 * add - function that adds 2 numbers
 * @a: represent first integer
 * @b: represent second integer
 * Return: always 0
 */
int add(int a, int b)
{
	int sum;
	sum = a + b;
	return (sum);
}
